{
  "name": "@showmax/client-side-graphql",
  "version": "1.0.0",
  "description": "Example setup of a client-side GraphQL schema that uses an existing REST backend",
  "engines": {
    "node": "^8.0.0"
  },
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "unbootstrap": "lerna clean",
    "docker:build": "lerna run docker:build",
    "start:docker": "docker-compose up"
  },
  "keywords": [
    "graphql",
    "REST",
    "apollo",
    "react"
  ],
  "author": "Showmax Frontend <frontend@showmax.com>",
  "license": "Apache-2.0",
  "private": true,
  "devDependencies": {
    "lerna": "^2.0.0"
  }
}
